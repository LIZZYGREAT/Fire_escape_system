# backend/core/config.py

# --- 全局数学常量定义 ---
INF = float('inf')

# --- 1. 状态空间与方向映射 ---
# 严格按照顺时针定义方向（适配左上角原点的图像坐标系）
DIR_N = 0  # 北 (y - 1)
DIR_E = 1  # 东 (x + 1)
DIR_S = 2  # 南 (y + 1)
DIR_W = 3  # 西 (x - 1)

# 方向的维度数量，用于初始化高维 NumPy 数组
NUM_DIRS = 4

# 虚拟超级源点的特定标识坐标 (x, y, theta)
SUPER_NODE = (-1, -1, -1)

# --- 2. 核心代价权重系统 ---
# 平地无风险走廊上，前进一步的基础物理代价
W_BASE = 1.0

# 转弯惩罚代价：90度转弯相当于直行2步
W_TURN = 2.0

# 原地掉头惩罚：绝对禁止反复横跳，物理意义为无穷大
W_REVERSE = INF

# --- 3. 风险与系统状态机阈值 ---
# 生命极限熔断阈值：当到达出口的最小代价超过此值时，触发小黑盒 SOS 红闪报警
T_FATAL = 500.0

# 危险权重极值上限 (Max-Pooling 的溢出天花板限制)
W_FIRE_MAX = 100.0