fire_escape_system/
│
├── backend/                        # 【云端重度计算层】Python
│   ├── data/                       # 静态数据与资产
│   │   ├── map.png                # 原始 CAD 建筑底图
│   │   └── M_mask.npy              # 预处理生成的 0/1 数学掩码矩阵
│   │
│   ├── core/                       # 核心算法引擎 (纯数学逻辑，无网络通信)
│   │   ├── __init__.py
│   │   ├── dstar_engine.py         # D* Lite 寻路管线 (状态升维、优先队列)
│   │   ├── fire_system.py          # 火势演化引擎 (元胞自动机/高斯矩阵卷积)
│   │   ├── lbb_manager.py          # 小黑盒逻辑模拟
│   │   ├── system_controller.py    # 调控管理器，作为dstar和fire_system的交互核心
│   │   └── config.py               # 统一数值管理
│   │
│   ├── scripts/                    # 离线工具链
│   │   └── map_processor.py        # OpenCV 图像清洗与矩阵生成脚本 (上一步写的)
│   │
│   ├── main.py                     # 后端入口：FastAPI/WebSocket 服务器，负责粘合 Core 与网络
│   └── requirements.txt            # Python 依赖清单 (numpy, scipy, fastapi, uvicorn, opencv-python)
│
└── frontend/                       # 【边缘视觉渲染层】HTML5/JS
    ├── index.html                  # 唯一的前端入口，定义 Canvas 容器和 UI 遮罩
    ├── css/
    │   └── style.css               # 样式文件 (深色大屏 UI 设计，保证 Canvas 满屏)
    │
    ├── assets/                     # 前端静态资源
    │   └── map_bg.png              # 经过透明化处理的地图底图 (用于 Canvas 垫底)
    │
    ├── main.js                  // 总控：管理基线同步与绘图调度 
    ├── network.js               // 通信：解析 Payload，提取黑盒指令 Diff 
    ├── renderer.js              // 渲染：负责绘制静态底图、热力场、HUD 界面 
    ├── lbb_visual.js            // 新增：绘制小黑盒的探测圆、指示箭头与 SOS 状态
    └── spline_engine.js         // 新增：负责将黑盒间的逻辑方向转化为平滑的路径链条